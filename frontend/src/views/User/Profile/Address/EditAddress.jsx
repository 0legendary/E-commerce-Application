import React from 'react'

function EditAddress({handleCancel}) {
  return (
    <div>
        <button onClick={handleCancel}>cancle</button>
      Edit Address
    </div>
  )
}

export default EditAddress
